<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.waldo.notesbites">
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".presentation.guesthomepage.GuestHomepageActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".presentation.module.ModuleActivity"/>

        <activity android:name=".presentation.subjectoverview.SubjectOverviewActivity"
            android:configChanges="orientation"
            />

        <activity android:name=".presentation.selectsubjects.SelectSubjectsActivity"/>

        <activity android:name=".presentation.homepage.HomepageActivity" android:label="Homepage"/>
        <activity android:name=".presentation.notes.NotesActivity"/>
        <activity android:name=".presentation.startingquiz.StartingQuizActivity"/>

        <activity android:name=".presentation.simplesubjectoverview.SimpleSubjectOverviewActivity"/>



        <activity android:name=".presentation.quiz.QuizActivity"/>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />

    </application>

</manifest>